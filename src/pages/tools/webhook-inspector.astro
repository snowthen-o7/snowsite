---
import Layout from '../../layouts/Layout.astro';
import WebhookInspector from '../../components/WebhookInspector/index';
---

<Layout title="Webhook Inspector" description="Capture, inspect, and debug incoming webhooks in real-time. Perfect for API development and testing." wide disableParticles>
  <div class="inspector-page">
    <header class="hero">
      <h1>Webhook Inspector</h1>
      <p class="subtitle">
        Capture, inspect, and debug webhooks in real-time.
      </p>
      <div class="feature-badges">
        <span class="badge">
          <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="12" cy="12" r="10"></circle>
            <polyline points="12 6 12 12 16 14"></polyline>
          </svg>
          Real-time updates
        </span>
        <span class="badge">
          <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"></path>
            <path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"></path>
          </svg>
          Unique endpoint URLs
        </span>
        <span class="badge">
          <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <rect x="3" y="3" width="18" height="18" rx="2" ry="2"></rect>
            <line x1="3" y1="9" x2="21" y2="9"></line>
            <line x1="9" y1="21" x2="9" y2="9"></line>
          </svg>
          Full request inspection
        </span>
      </div>
    </header>

    <div class="inspector-wrapper">
      <WebhookInspector client:load />
    </div>

    <section class="how-it-works">
      <h2>How It Works</h2>
      <div class="steps-grid">
        <div class="step">
          <span class="step-num">1</span>
          <h3>Create Endpoint</h3>
          <p>Generate a unique webhook URL that you can use in any service.</p>
        </div>
        <div class="step">
          <span class="step-num">2</span>
          <h3>Configure Webhook</h3>
          <p>Point your service's webhook settings to your unique URL.</p>
        </div>
        <div class="step">
          <span class="step-num">3</span>
          <h3>Inspect Requests</h3>
          <p>See every request in real-time with full headers and body content.</p>
        </div>
      </div>
    </section>

    <section class="use-cases">
      <h2>Perfect For</h2>
      <ul>
        <li>Testing webhook integrations during development</li>
        <li>Debugging payment provider callbacks</li>
        <li>Inspecting e-commerce platform notifications</li>
        <li>Verifying API request formats</li>
        <li>Troubleshooting third-party integrations</li>
      </ul>
    </section>
  </div>
</Layout>

<style>
  .inspector-page {
    max-width: 100%;
  }

  .hero {
    text-align: center;
    margin-bottom: 2rem;
  }

  .hero h1 {
    background: linear-gradient(135deg, #dc2626, var(--accent));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 0.5rem;
  }

  .subtitle {
    color: var(--text-muted);
    font-size: 1.1rem;
    margin-bottom: 1rem;
  }

  .feature-badges {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 0.5rem;
  }

  .badge {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.3rem 0.6rem;
    background: var(--bg-elevated);
    border: 1px solid var(--border);
    color: var(--text-muted);
    border-radius: 4px;
    font-family: var(--font-mono);
    font-size: 0.7rem;
  }

  .inspector-wrapper {
    background: var(--bg-elevated);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 1.5rem;
    margin-bottom: 3rem;
    min-height: 400px;
  }

  .how-it-works {
    margin-bottom: 3rem;
  }

  .how-it-works h2 {
    text-align: center;
    margin-bottom: 1.5rem;
  }

  .steps-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
  }

  @media (max-width: 640px) {
    .steps-grid {
      grid-template-columns: 1fr;
    }
  }

  .step {
    background: var(--bg-elevated);
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 1.25rem;
    text-align: center;
  }

  .step-num {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2rem;
    height: 2rem;
    background: linear-gradient(135deg, #dc2626, var(--accent));
    color: white;
    border-radius: 50%;
    font-family: var(--font-mono);
    font-size: 0.9rem;
    font-weight: 600;
    margin-bottom: 0.75rem;
  }

  .step h3 {
    font-size: 1rem;
    margin: 0 0 0.5rem 0;
  }

  .step p {
    font-size: 0.875rem;
    color: var(--text-muted);
    margin: 0;
    line-height: 1.5;
  }

  .use-cases {
    background: var(--bg-elevated);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 1.5rem 2rem;
  }

  .use-cases h2 {
    text-align: center;
    margin-top: 0;
    margin-bottom: 1rem;
  }

  .use-cases ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 0.5rem;
  }

  @media (max-width: 640px) {
    .use-cases ul {
      grid-template-columns: 1fr;
    }
  }

  .use-cases li {
    position: relative;
    padding-left: 1.5rem;
    color: var(--text-muted);
    font-size: 0.9rem;
  }

  .use-cases li::before {
    content: "→";
    position: absolute;
    left: 0;
    color: var(--accent);
  }
</style>
